import { toPhpModel } from './model-php';
import { version, url, formats } from './version';

globalThis.toPhpModel = toPhpModel;
globalThis.version = version;
globalThis.url = url;
globalThis.formats = formats;
